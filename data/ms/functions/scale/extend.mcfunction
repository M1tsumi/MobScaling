# Entry point for custom scaling hooks.
# Third-party datapacks or future updates can append functions to the
# `ms:scale_extend_hooks` tag to inject equipment, effects, or behavior
# without altering the core pipeline.
function #ms:scale_extend_hooks
